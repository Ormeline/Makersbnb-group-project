<!DOCTYPE html>
<html>
<head>
  <title>Your Requests</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 20px;
    }
    
    h1 {
      color: #333;
      margin-top: 0;
    }
    
    h2 {
      color: #333;
      margin-bottom: 20px;
    }
    
    .request-section {
      background-color: #fff;
      padding: 20px;
      box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
      margin-bottom: 40px;
    }
  </style>
</head>
<body>
  <h1>Your Requests</h1>
  
  <div class="request-section">
    <h2>Sent</h2>
      <% @requests.each do |object| %>
            <li>
                <div>
                    
                    <h2> Date: </h2>
                    <h2><%= object.date %></h2>
                    <h2> Property name: </h2>
       <h2><%= @listings.find(object.from_user_id).name %></h2>
                    <h2> To owner: </h2>
       <h2><%= @users.find(object.from_user_id).username %></h2>
                  
                </div>
            </li>

        <% end %>

  </div>
  
  <div class="request-section">
    <h2>Received</h2>
    <!-- Add your content for the "Received" requests here -->
  </div>
</body>
</html>
